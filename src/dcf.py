# Valorisation DCF
import pandas as pd
def calculate_wacc(equity_value: float, debt_value: float, cost_of_equity: float, cost_of_debt: float, tax_rate: float) -> float:
    """Calcule le WACC."""

def project_cash_flows(financial_data: pd.DataFrame, growth_rate: float, years: int = 5) -> list:
    """Projette les flux de trésorerie futurs."""

def dcf_valuation(projected_cash_flows: list, wacc: float, terminal_growth_rate: float) -> float:
    """Calcule la valeur actuelle nette via DCF."""
