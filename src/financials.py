# Calculs financiers (ratios, EBITDA, FCF)
import pandas as pd
def calculate_ebitda(income_statement: pd.DataFrame) -> float:
    """Calcule l'EBITDA à partir du compte de résultat."""

def calculate_ratios(financial_data: pd.DataFrame) -> dict:
    """Retourne les ratios clés : ROIC, Debt/EBITDA, Current Ratio, etc."""

def free_cash_flow(cash_flow_statement: pd.DataFrame) -> float:
    """Calcule le Free Cash Flow (FCF)."""
